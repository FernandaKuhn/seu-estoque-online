import { Typography } from '@mui/material';
import { StyledFooter } from './Footer.styles';

export const Footer = () => {
  return (
    <StyledFooter>
      <Typography variant="body2">Seu Estoque Online por Fernanda Kuhn</Typography>
    </StyledFooter>
  );
};